[tox]
envlist = py35,py36,check

[testenv]
setenv =
    PYTHONPATH = {toxinidir}:{toxinidir}/
deps =
    Flask
    gevent
    keras
    h5py
    imutils
    numpy
    Pillow
    redis
    scipy
    tensorflow
    .[dev]
    alembic
    bandit
    codecov
    flask-webtest
    flake8
    flake8-quotes
    isort
    pytest

commands =
    python -m pytest --cov=padma padma/tests

[testenv:check]
commands =
    flake8 padma --builtins='_' 
    isort padma --recursive --diff
    bandit -r padma -x tests/ -s B301,B403
    
